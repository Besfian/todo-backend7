db:
 url: jdbc:postgresql://localhost:5433/postgres
 username: postgres
 password: rootroot
 driver-class-name: org.postgresql.Driver



spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: rootroot

